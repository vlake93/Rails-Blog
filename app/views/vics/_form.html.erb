<%= form_with(model: vic) do |form| %>
  <% if vic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vic.errors.count, "error") %> prohibited this vic from being saved:</h2>

      <ul>
        <% vic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :about, style: "display: block" %>
    <%= form.text_field :about %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
